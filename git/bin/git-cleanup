#!/bin/bash
#
# Usage: git-cleanup
#
# Remove remote branch references that no longer exist, cleanup unnecessary git
# files, remove untracked files from the working tree and clear out your stash.

git remote prune origin
git gc
git clean -dfx
git stash clear
