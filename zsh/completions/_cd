#compdef cd

local curcontext="$curcontext" line ret=1 state

_arguments -C '1: :->projects' && ret=0

case $state in
  projects)
    _path_files -/ -W "${PROJECT_HOME}/${GITHUB_USER}"
    _path_files -/ -W "$PROJECT_HOME" -F "(${GITHUB_USER})"
    _path_files -/ .
    ret=0
  ;;
esac

return ret
